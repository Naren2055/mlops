---
title: Wellness Tourism Purchase Predictor
emoji: 🧳
colorFrom: blue
colorTo: indigo
sdk: docker
app_port: 8501
startup_duration_timeout: 45m
---

This Space uses the **Docker** SDK (`Dockerfile` + Streamlit), matching the course case study **port 8501**. `app_port` here must match `--server.port` in the `Dockerfile`.

Upload this folder with `tourism_project/hosting/hosting.py` so `README.md` is at the **repository root** next to `Dockerfile` and `app.py`.
